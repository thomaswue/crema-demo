# Build workbench

This folder is optional. It contains the scripts, configuration, and sample
programs used while building the Crema source launcher with GraalVM Native
Image.

The top-level demo does not depend on this folder at runtime. Generated build
outputs are intentionally ignored so the published repo stays focused on the
launcher comparison.
